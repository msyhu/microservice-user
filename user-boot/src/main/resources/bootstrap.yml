server:
  port: 8081
  
spring:
  application:
    name: user
#  cloud:
#    config:
#      uri: http://${configServerIp}:${configServerPort}/config/
#      fail-fast: true
  profiles:
    active: ${activatedProperties}
encrypt:
  failOnError: false

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    enabled: true
